# Component catalog (agent reference)

> **Auto-generated.** Do not edit this file. Run `npm run generate:component-catalog` after changing `componentRegistry.ts`, `types/cms.ts`, `config/component-affordances.json`, block schemas, or primitive components.

Generated: `2026-08-04T19:38:14.467Z`

## How to use this catalog

- **Tenant pages (CMS JSON):** pick blocks by `_type` below. Dispatch is via `components/componentRegistry.ts` → `PageRenderer`.
- **When to use what:** each entry lists **Use when** / **Avoid when** from `config/component-affordances.json` (edit that file when adding components).
- **New block:** `types/cms.ts`, `components/blocks/*.tsx`, `componentRegistry.ts`, schema, affordances entry, then `npm run generate:component-catalog`. See `docs/blocks.md`.
- **Primitives:** build or extend blocks — not CMS `_type` values. Sections are usually wrapped by `*Block` components.

## Health check

All catalog contract checks passed.

## CMS blocks (quick reference)

| _type | Summary | Schema |
| --- | --- | --- |
| `blog_list` | Blog index or news listing page when the tenant has the blog feature enabled. | yes |
| `breadcrumb` | Nested page hierarchies (e.g. /services/consulting) where users need parent context. | yes |
| `carouselBlock` | Rotating image galleries, testimonial strips, or card highlights on marketing pages. | yes |
| `caseStudiesBlock` | Portfolio or success-stories index linking to individual case study pages. | yes |
| `caseStudyDetailBlock` | Full case study article: challenge, solution, tech stack, impact metrics. | yes |
| `contact` | All-in-one contact page with info cards plus embedded messaging form. | yes |
| `contactFormSection` | Standalone lead-capture form section (name, email, message). | yes |
| `contactInfoBlock` | Contact page with form plus email, phone, and address in a side column. | yes |
| `ctaBlock` | Mid-page or bottom-of-page conversion push with headline and single CTA. | yes |
| `divider` | Visual separation between two block regions on the same page. | yes |
| `faqBlock` | Accordion FAQ on contact, pricing, or booking pages. | yes |
| `featureGridBlock` | Product or platform benefits in a scannable multi-column grid. | yes |
| `footer` | Site-wide footer with link columns and copyright on every page. | yes |
| `hero` | Simple centered hero: title, optional subtitle, one CTA. | yes |
| `heroBlock` | Homepage hero with large imagery and primary/secondary CTAs. | yes |
| `location` | Address, hours, and optional embedded map for visit-us pages. | yes |
| `logoCloud` | Trusted-by logo rows, press mentions, or partner strips. | yes |
| `missionBlock` | About page vision or mission statement with optional supporting image. | yes |
| `navbar` | Primary site header: logo, nav links, optional CTA. | yes |
| `pricedServicesBlock` | Homepage or landing page grid showing key services with price tags and direct WhatsApp CTAs. | yes |
| `pricingPageBlock` | Dedicated pricing page: tiers, promotion banner, testimonials, and FAQ in one block. | yes |
| `productExampleBlock` | Centered product proof: one heading and one image (e.g. phone mockup of a live site). | yes |
| `reservationBlock` | Guest booking funnel: service pick, date/time, contact, confirmation. | yes |
| `services` | Service catalog cards on homepage or menu page with booking CTAs. | yes |
| `servicesPageBlock` | Full services marketing page: hero, feature categories, service cards, FAQ. | yes |
| `statsBlock` | Key metrics row (users, uptime, locations) for credibility. | yes |
| `teamBlock` | About page team grid with photo, role, and bio per member. | yes |
| `testimonialsBlock` | Homepage or landing quote grid/slider with avatars and star ratings. | yes |
| `testimonialsPageBlock` | Dedicated reviews page with stats, featured quotes, logo cloud, and full list. | yes |
| `valuesBlock` | About page grid of core values or principles with icons. | yes |

## CMS blocks (full affordances)

### `blog_list`

- **Component:** `components/blocks/BlogListBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/blog_list.schema.json`
- **Use when:**
  - Blog index or news listing page when the tenant has the blog feature enabled.
  - Paginated grid of posts with thumbnails and read-time metadata.
- **Avoid when:**
  - Single static article content — use a custom page or rich blocks instead.

### `breadcrumb`

- **Component:** `components/blocks/BreadcrumbBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/breadcrumb.schema.json`
- **Use when:**
  - Nested page hierarchies (e.g. /services/consulting) where users need parent context.
  - Placed near the top of content pages below the navbar.
- **Avoid when:**
  - Flat single-level sites with only the homepage and one contact page.

### `carouselBlock`

- **Component:** `components/blocks/CarouselBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/carouselBlock.schema.json`
- **Use when:**
  - Rotating image galleries, testimonial strips, or card highlights on marketing pages.
  - When you need arrows, indicators, or autoplay across multiple slides.
  - Schema note: A multi-mode carousel block. Recommended: ≤12 slides in image mode, ≤8 slides in card mode. A 1-item carousel renders as static content — use a dedicated image/card block instead.
- **Avoid when:**
  - A single static image or quote — use a simpler content block instead.

### `caseStudiesBlock`

- **Component:** `components/blocks/CaseStudiesBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/caseStudiesBlock.schema.json`
- **Use when:**
  - Portfolio or success-stories index linking to individual case study pages.
  - Grid of client work with cover image, industry, and summary.
- **Avoid when:**
  - One long case study narrative — use caseStudyDetailBlock on the detail page.

### `caseStudyDetailBlock`

- **Component:** `components/blocks/CaseStudyDetailBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/caseStudyDetailBlock.schema.json`
- **Use when:**
  - Full case study article: challenge, solution, tech stack, impact metrics.
  - Dedicated slug page under /case-studies/ or similar.
- **Avoid when:**
  - Listing multiple projects — use caseStudiesBlock.

### `contact`

- **Component:** `components/blocks/ContactBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/contact.schema.json`
- **Use when:**
  - All-in-one contact page with info cards plus embedded messaging form.
  - When phone, email, address, and form should appear together.
- **Avoid when:**
  - Split layout with sidebar info and main form only — use contactInfoBlock or contactFormSection.

### `contactFormSection`

- **Component:** `components/blocks/ContactFormSection.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/contactFormSection.schema.json`
- **Use when:**
  - Standalone lead-capture form section (name, email, message).
  - Full-width form without adjacent address/phone cards.
- **Avoid when:**
  - Form already inside contactInfoBlock — do not duplicate on the same page.

### `contactInfoBlock`

- **Component:** `components/blocks/ContactInfoBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/contactInfoBlock.schema.json`
- **Use when:**
  - Contact page with form plus email, phone, and address in a side column.
  - Company profile fallbacks merge into empty contact fields at build time.
- **Avoid when:**
  - Map-first location page — use location block; form-only — use contactFormSection.

### `ctaBlock`

- **Component:** `components/blocks/CTABlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/ctaBlock.schema.json`
- **Use when:**
  - Mid-page or bottom-of-page conversion push with headline and single CTA.
  - Between content sections on homepage or pricing adjacent pages.
- **Avoid when:**
  - Primary above-the-fold hero — use hero or heroBlock.

### `divider`

- **Component:** `components/blocks/DividerBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/divider.schema.json`
- **Use when:**
  - Visual separation between two block regions on the same page.
  - Light structural break without extra content.

### `faqBlock`

- **Component:** `components/blocks/FaqBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/faqBlock.schema.json`
- **Use when:**
  - Accordion FAQ on contact, pricing, or booking pages.
  - Answering objections before reservation or contact submission.
- **Avoid when:**
  - FAQ is part of a full pricing layout — pricingPageBlock includes its own FAQ region.

### `featureGridBlock`

- **Component:** `components/blocks/FeatureGridBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/featureGridBlock.schema.json`
- **Use when:**
  - Product or platform benefits in a scannable multi-column grid.
  - Homepage or landing sections with icon/title/description items.
  - Use variant list for non-interactive benefit scans (divider rows, no card chrome).
- **Avoid when:**
  - Bookable service menu with prices — use services or servicesPageBlock.

### `footer`

- **Component:** `components/blocks/FooterBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/footer.schema.json`
- **Use when:**
  - Site-wide footer with link columns and copyright on every page.
  - Typically the last block in page JSON or injected via client chrome.

### `hero`

- **Component:** `components/blocks/HeroBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/hero.schema.json`
- **Use when:**
  - Simple centered hero: title, optional subtitle, one CTA.
  - Inner pages (menu, about subpages) that need a compact banner.
- **Avoid when:**
  - Marketing homepage with background image and dual CTAs — use heroBlock.

### `heroBlock`

- **Component:** `components/blocks/HomepageHeroBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/heroBlock.schema.json`
- **Use when:**
  - Homepage hero with large imagery and primary/secondary CTAs.
  - Portfolio or SaaS-style above-the-fold landing sections.
- **Avoid when:**
  - Minimal single-CTA banner — use hero.

### `location`

- **Component:** `components/blocks/LocationBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/location.schema.json`
- **Use when:**
  - Address, hours, and optional embedded map for visit-us pages.
  - Build merges company profile address when block fields are empty.
- **Avoid when:**
  - No physical location or map — use contactInfoBlock only.

### `logoCloud`

- **Component:** `components/blocks/LogoCloudBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/logoCloud.schema.json`
- **Use when:**
  - Trusted-by logo rows, press mentions, or partner strips.
  - Social proof between hero and features on B2B landings.

### `missionBlock`

- **Component:** `components/blocks/MissionBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/missionBlock.schema.json`
- **Use when:**
  - About page vision or mission statement with optional supporting image.
  - Single narrative column focused on why the business exists.

### `navbar`

- **Component:** `components/blocks/NavbarBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/navbar.schema.json`
- **Use when:**
  - Primary site header: logo, nav links, optional CTA.
  - First block on most pages or provided via client header config.

### `pricedServicesBlock`

- **Component:** `components/blocks/PricedServicesBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/pricedServicesBlock.schema.json`
- **Use when:**
  - Homepage or landing page grid showing key services with price tags and direct WhatsApp CTAs.
  - Compact service overview section with optional link to full /servicios page.
- **Avoid when:**
  - Full-page standalone services catalog with category filters and FAQs — use servicesPageBlock on subpages instead.

### `pricingPageBlock`

- **Component:** `components/blocks/PricingPageBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/pricingPageBlock.schema.json`
- **Use when:**
  - Dedicated pricing page: tiers, promotion banner, testimonials, and FAQ in one block.
  - SaaS or membership plans with compare-at-a-glance cards.
- **Avoid when:**
  - Single inline pricing mention — use services or ctaBlock.

### `productExampleBlock`

- **Component:** `components/blocks/ProductExampleBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/productExampleBlock.schema.json`
- **Use when:**
  - Centered product proof: one heading and one image (e.g. phone mockup of a live site).
  - Homepage demo beat right after the hero when copy and CTAs already live elsewhere.
- **Avoid when:**
  - Narrative about/mission copy with body text — use missionBlock.
  - Multiple slides or a gallery — use carouselBlock.

### `reservationBlock`

- **Component:** `components/blocks/ReservationBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/reservationBlock.schema.json`
- **Use when:**
  - Guest booking funnel: service pick, date/time, contact, confirmation.
  - Reservas page or embedded modal from services catalog when booking feature is on.
- **Avoid when:**
  - Booking disabled for tenant or contact-only lead gen — use contactFormSection.

### `services`

- **Component:** `components/blocks/ServicesBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/services.schema.json`
- **Use when:**
  - Service catalog cards on homepage or menu page with booking CTAs.
  - Pulls admin booking-services catalog at build/runtime when configured.
- **Avoid when:**
  - Long-form services marketing page — use servicesPageBlock.

### `servicesPageBlock`

- **Component:** `components/blocks/ServicesPageBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/servicesPageBlock.schema.json`
- **Use when:**
  - Full services marketing page: hero, feature categories, service cards, FAQ.
  - Dense deliverables and pricing descriptors per offering.
- **Avoid when:**
  - Compact homepage teaser grid — use pricedServicesBlock.

### `statsBlock`

- **Component:** `components/blocks/StatsBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/statsBlock.schema.json`
- **Use when:**
  - Key metrics row (users, uptime, locations) for credibility.
  - White, gray, or dark background variants for alternating sections.

### `teamBlock`

- **Component:** `components/blocks/TeamBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/teamBlock.schema.json`
- **Use when:**
  - About page team grid with photo, role, and bio per member.
  - Ordered member list for salons, agencies, or clinics.

### `testimonialsBlock`

- **Component:** `components/blocks/TestimonialsBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/testimonialsBlock.schema.json`
- **Use when:**
  - Homepage or landing quote grid/slider with avatars and star ratings.
  - Social proof before a CTA or pricing section.
- **Avoid when:**
  - Full testimonials directory page — use testimonialsPageBlock.

### `testimonialsPageBlock`

- **Component:** `components/blocks/TestimonialsPageBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/testimonialsPageBlock.schema.json`
- **Use when:**
  - Dedicated reviews page with stats, featured quotes, logo cloud, and full list.
  - SEO-focused social proof hub.
- **Avoid when:**
  - Short quote strip on homepage — use testimonialsBlock.

### `valuesBlock`

- **Component:** `components/blocks/ValuesBlock.tsx`
- **TypeScript (`Block` union):** yes
- **JSON schema:** `config/schemas/blocks/valuesBlock.schema.json`
- **Use when:**
  - About page grid of core values or principles with icons.
  - Three to six cards explaining how the business operates.

## Primitives: layout

### `Container`

- **Path:** `components/layout/Container.tsx`
- **Storybook:** `Layout/Container`
- **Use when:**
  - Max-width wrapper and horizontal padding for block inner content.
  - Center content on wide viewports inside Section.

### `Divider`

- **Path:** `components/layout/Divider.tsx`
- **Storybook:** `Layout/Divider`
- **Use when:**
  - Horizontal rule inside cards or between stacked regions.
  - Prefer divider block for full-page section breaks.

### `Grid`

- **Path:** `components/layout/Grid.tsx`
- **Storybook:** `Layout/Grid`
- **Use when:**
  - Responsive multi-column layouts for cards, features, or galleries.
  - Repeated items in columns across breakpoints.

### `Modal`

- **Path:** `components/layout/Modal.tsx`
- **Storybook:** `Layout/Modal`
- **Use when:**
  - Overlay dialogs inside blocks (e.g. service detail, booking) with focus trap.
  - Not a CMS block — composed within ServicesBlock and similar.

### `Section`

- **Path:** `components/layout/Section.tsx`
- **Storybook:** `Layout/Section`
- **Use when:**
  - Full-width band with background color and vertical padding.
  - Alternating white/gray/dark page regions.

### `Stack`

- **Path:** `components/layout/Stack.tsx`
- **Storybook:** `Layout/Stack`
- **Use when:**
  - Vertical spacing between form fields or stacked text blocks.
  - Consistent gap without manual margin classes.

## Primitives: content

### `Alert`

- **Path:** `components/content/Alert.tsx`
- **Storybook:** `Content/Alert`
- **Use when:**
  - Inline success, error, or warning messages after form actions.
  - Feedback banners inside blocks — not for page-level heroes.

### `Avatar`

- **Path:** `components/content/Avatar.tsx`
- **Storybook:** `Content/Avatar`
- **Use when:**
  - Profile images in testimonials, team cards, or comments.
  - Shows initials fallback when photo URL is missing.

### `Badge`

- **Path:** `components/content/Badge.tsx`
- **Storybook:** `Content/Badge`
- **Use when:**
  - Status labels (New, Beta), category tags, or inline state chips.
  - Small metadata on cards and list items.

### `Heading`

- **Path:** `components/content/Heading.tsx`
- **Storybook:** `Content/Heading`
- **Use when:**
  - Semantic page and section titles (h1–h4) with design-system sizing.
  - Any block that needs a controlled heading level and color.

### `Image`

- **Path:** `components/content/Image.tsx`
- **Storybook:** `Content/Image`
- **Use when:**
  - Responsive photos and illustrations with optional rounding.
  - Product shots and content imagery inside blocks.

### `Text`

- **Path:** `components/content/Text.tsx`
- **Storybook:** `Content/Text`
- **Use when:**
  - Body copy, descriptions, and supporting paragraphs.
  - Muted or emphasized variants for secondary copy.

## Primitives: data

### `Card`

- **Path:** `components/data/Card.tsx`
- **Storybook:** `Data/Card`
- **Use when:**
  - Bordered content grouping: blog previews, team members, service details.
  - Contained panels inside Section/Container layouts.

### `List`

- **Path:** `components/data/List.tsx`
- **Storybook:** `Data/List`
- **Use when:**
  - Bulleted or numbered feature lists and step sequences.
  - When a table would be heavier than needed.

### `Table`

- **Path:** `components/data/Table.tsx`
- **Storybook:** `Data/Table`
- **Use when:**
  - Labeled rows and columns for comparisons or tabular data.
  - Spec sheets and structured exports in content blocks.

## Primitives: inputs

### `Button`

- **Path:** `components/inputs/Button.tsx`
- **Storybook:** `Inputs/Button`
- **Use when:**
  - Primary, secondary, and destructive actions in forms and CTAs.
  - Navigation and submit triggers inside blocks.

### `Checkbox`

- **Path:** `components/inputs/Checkbox.tsx`
- **Storybook:** `Inputs/Checkbox`
- **Use when:**
  - Boolean form options such as terms acceptance or feature toggles.

### `ContactForm`

- **Path:** `components/inputs/ContactForm.tsx`
- **Storybook:** —
- **Use when:**
  - Standard name/email/message form when composing custom contact UIs.
  - Prefer contactFormSection block for tenant pages unless building a new block.

### `Input`

- **Path:** `components/inputs/Input.tsx`
- **Storybook:** `Inputs/Input`
- **Use when:**
  - Single-line fields: name, email, search, phone in custom forms.

### `Select`

- **Path:** `components/inputs/Select.tsx`
- **Storybook:** `Inputs/Select`
- **Use when:**
  - Dropdown for one-of-many choices: country, category, service filter.

### `Textarea`

- **Path:** `components/inputs/Textarea.tsx`
- **Storybook:** `Inputs/Textarea`
- **Use when:**
  - Multi-line message or notes fields in forms.

## Primitives: navigation

### `BottomCtaBar`

- **Path:** `components/navigation/BottomCtaBar.tsx`
- **Storybook:** `Navigation/BottomCtaBar`
- **Use when:**
  - Fixed mobile bottom bar with tel:, mailto:, WhatsApp, or path links.
  - Configured in client.json — not a CMS page block.

### `Breadcrumb`

- **Path:** `components/navigation/Breadcrumb.tsx`
- **Storybook:** `Navigation/Breadcrumb`
- **Use when:**
  - Primitive breadcrumb trail inside custom layouts.
  - Prefer breadcrumb block for tenant pages.

### `Footer`

- **Path:** `components/navigation/Footer.tsx`
- **Storybook:** `Navigation/Footer`
- **Use when:**
  - Primitive footer markup inside composite blocks.
  - Prefer footer block or client footer config for full pages.

### `Navbar`

- **Path:** `components/navigation/Navbar.tsx`
- **Storybook:** `Navigation/Navbar`
- **Use when:**
  - Primitive header inside page-level blocks.
  - Prefer navbar block for standard tenant chrome.

### `NavLink`

- **Path:** `components/navigation/NavLink.tsx`
- **Storybook:** `Navigation/NavLink`
- **Use when:**
  - Single nav item with active state inside Navbar compositions.

## Primitives: sections

### `AnimatedStatValue`

- **Path:** `components/sections/AnimatedStatValue.tsx`
- **Storybook:** —
- **Notes:** internal helper — no affordances required

### `Carousel`

- **Path:** `components/sections/Carousel.tsx`
- **Storybook:** `Sections/Carousel`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Section-level carousel used by carouselBlock.
  - Do not reference from page JSON — use carouselBlock _type.

### `CTA`

- **Path:** `components/sections/CTA.tsx`
- **Storybook:** `Sections/CTA`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Section CTA layout composed inside ctaBlock.
  - Use ctaBlock in page JSON, not this component directly.

### `FAQ`

- **Path:** `components/sections/FAQ.tsx`
- **Storybook:** `Sections/FAQ`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Accordion FAQ section inside faqBlock and pricing layouts.
  - Use faqBlock in page JSON for tenant pages.

### `FeatureGrid`

- **Path:** `components/sections/FeatureGrid.tsx`
- **Storybook:** `Sections/FeatureGrid`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Feature grid section inside featureGridBlock and servicesPageBlock.
  - Use featureGridBlock in page JSON.
  - Pass variant list for divider-row benefit lists without card chrome.

### `Hero`

- **Path:** `components/sections/Hero.tsx`
- **Storybook:** `Sections/Hero`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Hero section layout inside heroBlock and servicesPageBlock.
  - Use hero or heroBlock in page JSON for tenant pages.

### `LogoCloud`

- **Path:** `components/sections/LogoCloud.tsx`
- **Storybook:** `Sections/LogoCloud`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Logo row section inside logoCloud and testimonialsPageBlock.
  - Use logoCloud block in page JSON.

### `PricingTable`

- **Path:** `components/sections/PricingTable.tsx`
- **Storybook:** `Sections/PricingTable`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Pricing tiers inside pricingPageBlock.
  - Use pricingPageBlock in page JSON.

### `StatsBar`

- **Path:** `components/sections/StatsBar.tsx`
- **Storybook:** `Sections/StatsBar`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Stats row inside statsBlock and testimonialsPageBlock.
  - Use statsBlock in page JSON.

### `Testimonials`

- **Path:** `components/sections/Testimonials.tsx`
- **Storybook:** `Sections/Testimonials`
- **Notes:** prefer the matching CMS `*Block` in page JSON
- **Use when:**
  - Testimonial layout inside testimonialsBlock and pricingPageBlock.
  - Use testimonialsBlock in page JSON.

